name: "Check PR title"

on:
  pull_request:
    types:
      - opened
      - reopened
      - edited
      - synchronize

jobs:
  main:
    uses: health-informatics-uon/workflows/.github/workflows/pr-title.yml@e38f273dc42a4e0af8a10ac328ad5b5b631fe416 # v 1.4.9
    permissions:
      pull-requests: read
