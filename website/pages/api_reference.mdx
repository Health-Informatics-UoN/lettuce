# API reference

The main Lettuce API has a [full reference](api_reference/lettuce)

Using Lettuce also requires that you have an embeddings table in your database.
A small library for building your own embeddings is [also provided](api_reference/build_embeddings).
